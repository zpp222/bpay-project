<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.zpp.service.bpayservice.dto.User">
    <select id="select" parameterType="String" resultType="com.zpp.service.bpayservice.dto.User">
        SELECT * FROM user WHERE name = #{name}
    </select>
</mapper>