<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zpp.service.bpayservice.mapper.UserMapper">
    
    <select id="selectByName" parameterType="String" resultType="org.bpay.serviceI.dto.User">
        SELECT * FROM user WHERE name = #{name}
    </select>
    
</mapper>