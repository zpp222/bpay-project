spring:
  application:
    name: bpay-client
  cloud:
    config:
      uri: http://localhost:9021/
      profile: dev
      label: master
management:
  endpoints:
    web:
      exposure:
        include: '*'